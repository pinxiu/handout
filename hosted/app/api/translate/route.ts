export async function POST(request:Request){try{const{text}=await request.json()as{text?:string};if(!text?.trim())return Response.json({error:"The document has no readable text."},{status:400});const apiKey=process.env.GEMINI_API_KEY;if(!apiKey)return Response.json({error:"Translation is not configured yet."},{status:503});const prompt=`Translate this English church handout into natural Simplified Chinese. Return JSON only: {"title":"","blocks":[{"type":"heading|paragraph|question|notes|scripture","english":"original text","chinese":"translation"}]}. Mark every Bible verse/passage block as scripture. Preserve every section, paragraph, question, blank prompt, and Bible verse exactly once and in source order. Use Chinese punctuation. Bible labels say 和合本, never 和合本简体. Normalize 〔〕 to （）, 「」 to “”, and 『』 to ‘’. SOURCE:\n${text}`;const response=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent?key=${apiKey}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({contents:[{parts:[{text:prompt}]}],generationConfig:{responseMimeType:"application/json"}})}),json=await response.json()as any;if(!response.ok)throw new Error(json.error?.message||"Gemini translation failed.");return Response.json(JSON.parse(json.candidates?.[0]?.content?.parts?.[0]?.text))}catch(error:any){return Response.json({error:error.message||"Translation failed."},{status:500})}}
