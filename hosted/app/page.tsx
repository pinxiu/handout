"use client";
import {useState} from"react";
import * as mammoth from"mammoth";
import{AlignmentType,BorderStyle,Document,Header,Packer,PageOrientation,Paragraph,Table,TableCell,TableRow,TextRun,WidthType}from"docx";
type Block={type:"heading"|"paragraph"|"question"|"notes";english:string;chinese:string};type Draft={title:string;blocks:Block[]};const noBorder={style:BorderStyle.NONE,size:0,color:"FFFFFF"};
async function readFile(file:File){const data=await file.arrayBuffer();if(file.name.toLowerCase().endsWith(".docx"))return(await mammoth.extractRawText({arrayBuffer:data})).value;if(file.name.toLowerCase().endsWith(".pdf")){const pdfjs=await import("pdfjs-dist");pdfjs.GlobalWorkerOptions.workerSrc=`https://cdn.jsdelivr.net/npm/pdfjs-dist@${pdfjs.version}/build/pdf.worker.min.mjs`;const pdf=await pdfjs.getDocument({data}).promise,pages:string[]=[];for(let n=1;n<=pdf.numPages;n++){const page=await pdf.getPage(n),content=await page.getTextContent();pages.push(content.items.map(item=>"str"in item?item.str:"").join(" "))}return pages.join("\n\n")}throw new Error("Choose a DOCX or text-based PDF.")}
async function translate(text:string){const response=await fetch("/api/translate",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({text})}),json=await response.json();if(!response.ok)throw new Error(json.error||"Translation failed.");return json as Draft}
const run=(text:string,chinese=false,bold=false,size=19)=>{const font=chinese?"Noto Sans SC":"Arial";return new TextRun({text,bold,size,font:{ascii:font,hAnsi:font,eastAsia:font}})};
async function makeDocx(draft:Draft,o:any){const bilingual=o.mode==="bilingual",landscape=o.orientation==="landscape",side=bilingual&&o.arrangement==="side",pageWidth=landscape?15840:12240,width=pageWidth-2160,half=Math.floor(width/2),children:(Paragraph|Table)[]=[];if(o.title.trim())children.push(new Paragraph({children:[run(o.title.trim(),/[\u3400-\u9fff]/.test(o.title),true,28)],spacing:{after:160}}));for(const block of draft.blocks){const heading=block.type==="heading"||block.type==="notes",en=new Paragraph({children:[new TextRun({text:block.english,bold:heading,size:heading?22:19,font:o.englishFont})],spacing:{after:block.type==="question"?180:45,line:240}}),zh=new Paragraph({children:[new TextRun({text:block.chinese,bold:heading,size:heading?22:19,font:{ascii:o.chineseFont,hAnsi:o.chineseFont,eastAsia:o.chineseFont}})],spacing:{after:block.type==="question"?180:45,line:280}});if(!bilingual)children.push(o.mode==="chinese"?zh:en);else if(side)children.push(new Table({width:{size:width,type:WidthType.DXA},columnWidths:[half,width-half],borders:{top:noBorder,bottom:noBorder,left:noBorder,right:noBorder,insideHorizontal:noBorder,insideVertical:noBorder},rows:[new TableRow({children:[new TableCell({margins:{right:120},children:[en]}),new TableCell({margins:{left:120},children:[zh]})]})]}));else children.push(en,zh)}const header=new Header({children:[new Table({width:{size:width,type:WidthType.DXA},columnWidths:[half,width-half],borders:{top:noBorder,bottom:noBorder,left:noBorder,right:noBorder,insideHorizontal:noBorder,insideVertical:noBorder},rows:[new TableRow({children:[new TableCell({children:[new Paragraph(o.headerLeft)]}),new TableCell({children:[new Paragraph({alignment:AlignmentType.RIGHT,children:[run(o.headerRight,/[\u3400-\u9fff]/.test(o.headerRight))]})]})]})]})]});const doc=new Document({sections:[{properties:{page:{size:landscape?{width:15840,height:12240,orientation:PageOrientation.LANDSCAPE}:undefined,margin:{top:720,bottom:720,left:1080,right:1080,header:360,footer:360}}},headers:{default:header},children}]});return Packer.toBlob(doc)}
async function googleToken(clientId:string){if(!(window as any).google?.accounts?.oauth2)await new Promise<void>((resolve,reject)=>{const s=document.createElement("script");s.src="https://accounts.google.com/gsi/client";s.onload=()=>resolve();s.onerror=()=>reject(new Error("Google sign-in could not load."));document.head.appendChild(s)});return new Promise<string>((resolve,reject)=>{const client=(window as any).google.accounts.oauth2.initTokenClient({client_id:clientId,scope:"https://www.googleapis.com/auth/drive.file",callback:(r:any)=>r.error?reject(new Error(r.error)):resolve(r.access_token),error_callback:()=>reject(new Error("Google sign-in was cancelled."))});client.requestAccessToken({prompt:"consent"})})}
async function uploadGoogleDoc(blob:Blob,name:string,token:string){const boundary=`handout_${Date.now()}`,metadata=JSON.stringify({name,mimeType:"application/vnd.google-apps.document"}),body=new Blob([`--${boundary}\r\nContent-Type: application/json; charset=UTF-8\r\n\r\n${metadata}\r\n`,`--${boundary}\r\nContent-Type: application/vnd.openxmlformats-officedocument.wordprocessingml.document\r\n\r\n`,blob,`\r\n--${boundary}--`]);const response=await fetch("https://www.googleapis.com/upload/drive/v3/files?uploadType=multipart&fields=webViewLink",{method:"POST",headers:{Authorization:`Bearer ${token}`,"Content-Type":`multipart/related; boundary=${boundary}`},body}),json=await response.json();if(!response.ok)throw new Error(json.error?.message||"Google Drive upload failed.");return json.webViewLink as string}
export default function Home(){const[file,setFile]=useState<File|null>(null),[source,setSource]=useState(""),[draft,setDraft]=useState<Draft|null>(null),[busy,setBusy]=useState(""),[error,setError]=useState(""),[mode,setMode]=useState("bilingual"),[orientation,setOrientation]=useState("landscape"),[arrangement,setArrangement]=useState("side"),[title,setTitle]=useState(""),[headerLeft,setHeaderLeft]=useState(""),[headerRight,setHeaderRight]=useState(""),[englishFont,setEnglishFont]=useState("Arial"),[chineseFont,setChineseFont]=useState("Noto Sans SC");const googleClientId=process.env.NEXT_PUBLIC_GOOGLE_CLIENT_ID||"",options={mode,orientation,arrangement,title,headerLeft,headerRight,englishFont,chineseFont};async function importDocument(){if(!file)return;setBusy("Reading document…");setError("");try{setSource(await readFile(file))}catch(e:any){setError(e.message)}setBusy("")}async function generate(){setBusy("Translating with Gemini…");setError("");try{setDraft(await translate(source))}catch(e:any){setError(e.message)}setBusy("")}async function openGoogleDoc(){if(!draft)return;if(!googleClientId){setError("Google sign-in is not configured yet.");return}const target=window.open("about:blank","_blank");setBusy("Creating Google Doc…");setError("");try{const[blob,token]=await Promise.all([makeDocx(draft,options),googleToken(googleClientId)]),link=await uploadGoogleDoc(blob,title||draft.title||"Bilingual handout",token);if(target)target.location.href=link;else window.open(link,"_blank")}catch(e:any){target?.close();setError(e.message)}setBusy("")}
return <main><header className="topbar"><div className="brand"><span>H</span>Handout Bridge</div><div className="badge">English → 简体中文</div></header><section className="hero"><p className="kicker">Church handouts, without the formatting marathon</p><h1>Translate, align,<br/>and continue in Google Docs.</h1><p>Upload an English DOCX or PDF, review every translation, then create an editable Google Doc in your Drive.</p></section><section className="workspace"><div className="panel"><h2>1. Import</h2><label className="drop"><input type="file" accept=".docx,.pdf" onChange={e=>setFile(e.target.files?.[0]||null)}/><strong>{file?.name||"Choose DOCX or PDF"}</strong><small>Your document stays in this browser.</small></label><button disabled={!file||!!busy} onClick={importDocument}>{busy||"Read document"}</button>{source&&<><h2>2. Translate</h2><textarea value={source} onChange={e=>setSource(e.target.value)}/><button disabled={!!busy} onClick={generate}>{busy||"Generate bilingual handout"}</button></>}{draft&&<><h2>3. Format & open</h2><label>Title<input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Optional"/></label><label>Header left<input value={headerLeft} onChange={e=>setHeaderLeft(e.target.value)}/></label><label>Header right<input value={headerRight} onChange={e=>setHeaderRight(e.target.value)}/></label><div className="row"><label>Output<select value={mode} onChange={e=>setMode(e.target.value)}><option value="bilingual">Bilingual</option><option value="chinese">Chinese only</option><option value="english">English only</option></select></label><label>Page<select value={orientation} onChange={e=>setOrientation(e.target.value)}><option value="landscape">Landscape</option><option value="portrait">Portrait</option></select></label></div><label>Bilingual arrangement<select value={arrangement} onChange={e=>setArrangement(e.target.value)}><option value="side">Side by side</option><option value="stacked">English then Chinese</option></select></label><div className="row"><label>English font<input value={englishFont} onChange={e=>setEnglishFont(e.target.value)}/></label><label>中文字体<input value={chineseFont} onChange={e=>setChineseFont(e.target.value)}/></label></div><button className="google" disabled={!!busy} onClick={openGoogleDoc}>{busy||"Create & open Google Doc"} ↗</button></>}{error&&<p className="error">{error}</p>}</div><article className="preview">{!draft?<div className="empty"><span>双</span><h2>Your bilingual handout will appear here.</h2><p>Every generated line remains editable before it reaches Google Docs.</p></div>:<><input className="preview-title" value={title||draft.title} onChange={e=>setTitle(e.target.value)}/>{draft.blocks.map((block,index)=><div className={`pair ${block.type}`} key={index}>{mode!=="chinese"&&<textarea value={block.english} onChange={e=>setDraft({...draft,blocks:draft.blocks.map((b,i)=>i===index?{...b,english:e.target.value}:b)})}/>} {mode!=="english"&&<textarea lang="zh-Hans" value={block.chinese} onChange={e=>setDraft({...draft,blocks:draft.blocks.map((b,i)=>i===index?{...b,chinese:e.target.value}:b)})}/>}</div>)}</>}</article></section></main>}
